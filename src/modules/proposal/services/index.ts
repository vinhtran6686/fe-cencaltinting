// Proposal services using centralized API configuration

import { apiService } from '../../../services/apiService';
import { Proposal } from '../redux/proposalTypes';

const API_ENDPOINT = '/proposals';

export const proposalService = {
};

export default proposalService;
